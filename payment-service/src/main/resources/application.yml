spring:
  application:
    name: 'payment-service'
  datasource:
    url: 'jdbc:mysql://localhost:3308/bookstore?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false'
    username: 'root'
    password: '0000'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

server:
  port: 8084

# Eureka Server URL
eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:8761/eureka'
